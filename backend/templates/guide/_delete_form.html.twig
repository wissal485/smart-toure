<form method="post" action="{{ path('app_guide_delete', {'id': guide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide.id) }}">
    <button class="btn">Delete</button>
</form>
